import React from "react";

const DateChecker = (item) => {
  // today date object
  const today = new Date();

  var todayDate = new Date().toISOString().slice(0, 10);
  // console.log(todayDate);
  // item's date from the list of objects

  // check condition, see if the condition needs
  // to apply directly to the item itself,
  // instead of a property of it
};

export default DateChecker;
